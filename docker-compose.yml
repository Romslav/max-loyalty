version: '3.8'

services:
  app:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=http://localhost:3000/api
    command: npm run dev -- --host

  # Опционально: Backend API (если у вас есть)
  # api:
  #   image: your-api-image
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - DATABASE_URL=postgresql://user:password@db:5432/loyalty
  #   depends_on:
  #     - db

  # db:
  #   image: postgres:15
  #   environment:
  #     - POSTGRES_DB=loyalty
  #     - POSTGRES_USER=user
  #     - POSTGRES_PASSWORD=password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:
