# 🚀 GitHub Actions - Полное Руководство для Новичков

## 📚 ЧТО ЭТО ТАКОЕ?

**GitHub Actions** = автоматический тестер, который запускается когда ты:
- Пушишь код в GitHub
- Открываешь Pull Request (PR)
- Делаешь что-то с веткой

**Проще говоря:** GitHub сам запускает тесты за тебя 🤖

---

## 🎯 ЧТО СЕЙЧАС РАБОТАЕТ

В проекте уже настроены **2 автоматических процесса**:

### 1️⃣ **Tests Workflow** (`.github/workflows/tests.yml`)
- Запускается: когда ты пушишь код в `main` или `develop`
- Делает: запускает все тесты и проверки
- На каких версиях Node: 18.x и 20.x

### 2️⃣ **PR Quality Checks** (`.github/workflows/pr-checks.yml`)
- Запускается: когда ты открываешь Pull Request
- Делает: дополнительные проверки качества
- Пишет: комментарий в PR с результатами

---

## ⚡ ПОШАГОВАЯ ИНСТРУКЦИЯ

### ШАГИ НА ТВОЕМ КОМПЬЮТЕРЕ

#### Шаг 1: Склонируй репо

```bash
git clone https://github.com/Romslav/max-loyalty.git
cd max-loyalty
```

#### Шаг 2: Создай новую ветку для работы

```bash
# Создание и переход на новую ветку
git checkout -b feature/my-feature
```

**Примеры имен веток:**
- `feature/add-new-hook` - новая фича
- `fix/bug-in-auth` - исправление баги
- `docs/update-guide` - обновление документации

#### Шаг 3: Делай изменения в коде

Отредактируй файлы, добавь тесты, все как обычно.

#### Шаг 4: Проверь локально (опционально)

```bash
# Установи зависимости
npm install

# Запусти тесты локально
npm run test

# Или в watch режиме
npm run test:watch
```

#### Шаг 5: Сделай коммит

```bash
# Добавь все изменения
git add .

# Создай коммит с понятным сообщением
git commit -m "feat: Add new useCustom hook with tests"
```

**Примеры сообщений коммитов:**
- `feat: Add new feature`
- `fix: Fix bug in component`
- `test: Add tests for hook`
- `docs: Update README`
- `style: Fix formatting`

#### Шаг 6: Запушь код в GitHub

```bash
# Отправляем ветку на GitHub
git push origin feature/my-feature
```

---

## 🌐 НА GITHUB (ВЕБ-ИНТЕРФЕЙС)

### ВАРИАНТ А: Автоматический тест при PUSH'е

#### Шаг 1: Смотри статус

После `git push` зайди на:
```
https://github.com/Romslav/max-loyalty
```

Увидишь:
- Зеленая галочка ✅ = тесты прошли
- Красный крестик ❌ = тесты упали
- Желтая точка 🟡 = тесты еще запускаются

#### Шаг 2: Проверь детали

1. Нажми на иконку статуса (✅, ❌ или 🟡)
2. Нажми "Details" или "Show all checks"
3. Откроется GitHub Actions с деталями

**Или перейди по прямой ссылке:**
```
https://github.com/Romslav/max-loyalty/actions
```

#### Шаг 3: Посмотри результаты

Видишь workflow "Tests"
- Нажми на последний запуск
- Смотри статус каждого шага:
  - Install dependencies ✅
  - Run tests ✅
  - Generate coverage ✅
  - TypeScript check ✅
  - Run lint ✅

---

### ВАРИАНТ Б: Открытие Pull Request (PR)

#### Шаг 1: Создай PR на GitHub

1. Зайди в репозиторий: `https://github.com/Romslav/max-loyalty`
2. Увидишь желтый банер "Compare & pull request"
3. Нажми на него

**Или создай вручную:**
1. Вкладка "Pull requests"
2. Кнопка "New pull request"
3. Выбери:
   - base: `main` (куда вливаем)
   - compare: `feature/my-feature` (откуда берем)
4. Нажми "Create pull request"

#### Шаг 2: Заполни информацию

```markdown
# Title
Add new authentication hook

# Description
- What did you change?
- Why did you change it?
- Any side effects?

# Checklist
- [x] Tests passed
- [x] No console errors
- [x] Updated documentation
```

#### Шаг 3: Жди автоматических проверок

Увидишь блок "Checks" в PR:

```
✅ Tests (Ubuntu Latest, Node 18.x)
✅ Tests (Ubuntu Latest, Node 20.x)
✅ Code Quality
```

#### Шаг 4: Прочитай комментарий бота

Бот оставит комментарий с результатами:

```
## ✅ PR Quality Check Results

| Check | Status |
|-------|--------|
| Tests | ✅ Passed |
| TypeScript | ✅ Passed |
| Lint | ✅ Passed |
| Format | ✅ Passed |

### Coverage Report
- **Statements**: 87.2%

✨ Ready to merge!
```

---

## 🔍 КАК СМОТРЕТЬ РЕЗУЛЬТАТЫ

### Смотри логи тестов

1. Зайди в **Actions** вкладку
2. Выбери последний workflow
3. Кликни на "Run tests" или другой шаг
4. Видишь весь лог выполнения

**Пример лога:**
```bash
> npm run test

✓ src/hooks/__tests__/useAuth.test.ts (4)
✓ src/hooks/__tests__/usePermissions.test.ts (6)
✓ src/hooks/__tests__/useNotification.test.ts (5)
✓ src/hooks/__tests__/useAsync.test.ts (8)
✓ src/hooks/__tests__/useLocalStorage.test.ts (10)
✓ src/pages/admin/__tests__/AuditLogs.test.tsx (4)

Test Files  6 passed (6)
     Tests  45 passed (45)
  Duration  2.34s
```

### Скачай результаты

В workflow есть секция "Artifacts":
- Можешь скачать `test-results-18.x` или `test-results-20.x`
- Там будут coverage отчеты и логи

---

## ❌ ЧТО ДЕЛАТЬ ЕСЛИ ТЕСТЫ УПАЛИ?

### Сценарий 1: Красный крестик в Actions

```bash
# 1. Посмотри что сломалось в логах
# 2. Скопируй ошибку
# 3. Вернись к себе локально
git pull origin main  # обновись
git checkout feature/my-feature  # вернись на свою ветку

# 4. Запусти тесты локально
npm run test

# 5. Исправь ошибку в коде
# 6. Снова тестирование
npm run test:watch

# 7. Когда все работает - пушь
git add .
git commit -m "fix: Fix failing tests"
git push origin feature/my-feature
```

### Сценарий 2: Тесты упали в PR

1. GitHub напишет комментарий с ошибкой
2. Кликни "Show all checks" → найди падающий тест
3. Прочитай ошибку
4. Исправь локально
5. Пушь еще раз - PR автоматически обновится

---

## 💡 ПОЛЕЗНЫЕ СОВЕТЫ

### Совет 1: Хорошие коммиты

```bash
# ❌ Плохо
git commit -m "fix"
git commit -m "updates"

# ✅ Хорошо
git commit -m "fix: Fix authentication error in useAuth hook"
git commit -m "test: Add tests for usePermissions hook"
git commit -m "docs: Update testing guide"
```

### Совет 2: Часто пушь

```bash
# Лучше делать много маленьких коммитов
git commit -m "feat: Add step 1"
git push

git commit -m "feat: Add step 2"
git push

# Чем один большой
git commit -m "feat: Add everything"
git push
```

### Совет 3: Смотри Actions регулярно

После каждого push'а проверяй:
1. Тесты прошли? ✅
2. Нет предупреждений? ⚠️
3. Coverage не упал? 📊

### Совет 4: Читай комментарии бота

Бот будет писать:
- Статус тестов
- Coverage отчет
- Ошибки если есть
- Рекомендации

---

## 🎯 ПРИМЕР: ОТ А ДО Я

### Сценарий: Хочу добавить новый хук

#### На компьютере:

```bash
# Шаг 1: Обновляю код
git checkout -b feature/new-hook

# Шаг 2: Создаю файлы
# useNewHook.ts + useNewHook.test.ts

# Шаг 3: Тестирую локально
npm run test:watch
# Все работает ✅

# Шаг 4: Коммичу
git add .
git commit -m "feat: Add useNewHook with comprehensive tests"

# Шаг 5: Пушу
git push origin feature/new-hook
```

#### На GitHub (в браузере):

```
1. GitHub предлагает создать PR
2. Я нажимаю "Compare & pull request"
3. Заполняю описание
4. Нажимаю "Create pull request"
5. Жду 30 секунд...
6. GitHub Actions запускается автоматически
7. Видю все проверки: ✅ ✅ ✅
8. Бот пишет комментарий "Ready to merge!"
9. Нажимаю "Merge pull request"
10. Код влит в main! 🎉
```

---

## 🎓 ДАЛЬШЕ

После слияния PR в main:
- Ветка автоматически удалится
- Бэйдж на README обновится (если настроен)
- Production может деплоиться (если настроен)

---

## 📞 ВОПРОСЫ?

### Где смотреть логи?
→ GitHub → Actions → последний workflow → кликни на шаг

### Тесты упали, что делать?
→ Сначала локально `npm run test`, потом исправляй, потом пушь

### Как ускорить тесты?
→ Используй `--only-changed` или запускай конкретный файл

### Можно ли пропустить проверки?
→ Нет, это плохая практика 🚫 Всегда делай проверки!

---

## ✅ CHECKLIST ДЛЯ НОВИЧКА

- [ ] Понимаю что такое GitHub Actions
- [ ] Знаю где смотреть результаты (Actions вкладка)
- [ ] Могу создать свою ветку
- [ ] Могу сделать commit и push
- [ ] Могу создать PR
- [ ] Понимаю что делать если тесты упали
- [ ] Читаю комментарии бота
- [ ] Мои PR'ы проходят все проверки ✅

---

**Готов к работе!** 🚀
