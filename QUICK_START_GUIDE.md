# 🚀 MAX LOYALTY - ПОЛНОЕ РУКОВОДСТВО ПО ЗАПУСКУ

> Подробная пошаговая инструкция по запуску проекта Max Loyalty с нуля

---

## 📋 СОДЕРЖАНИЕ

1. [Требования к системе](#требования-к-системе)
2. [Установка необходимого ПО](#установка-необходимого-по)
3. [Клонирование проекта](#клонирование-проекта)
4. [Настройка Backend](#настройка-backend)
5. [Настройка Frontend](#настройка-frontend)
6. [Первый запуск](#первый-запуск)
7. [Тестовые аккаунты](#тестовые-аккаунты)
8. [Проверка работоспособности](#проверка-работоспособности)
9. [Устранение проблем](#устранение-проблем)

---

## 1️⃣ ТРЕБОВАНИЯ К СИСТЕМЕ

### Минимальные требования:

- **Операционная система:** Windows 10/11, macOS 10.15+, или Linux (Ubuntu 20.04+)
- **ОЗУ:** 4 GB (рекомендуется 8 GB)
- **Свободное место:** 2 GB
- **Процессор:** Dual-core 2.0 GHz или выше

### Необходимое ПО:

✅ **Node.js** - версия 18.0 или выше  
✅ **npm** - версия 8.0 или выше (идет с Node.js)  
✅ **Git** - версия 2.30 или выше  
✅ **PostgreSQL** - версия 14 или выше  
✅ **Текстовый редактор** - VS Code (рекомендуется) или любой другой  

---

## 2️⃣ УСТАНОВКА НЕОБХОДИМОГО ПО

### Windows:

#### Node.js
```powershell
# Скачайте с официального сайта:
# https://nodejs.org/en/download/
# Выберите "LTS" версию

# После установки проверьте:
node --version  # Должно показать v18.x.x или выше
npm --version   # Должно показать v8.x.x или выше
```

#### Git
```powershell
# Скачайте с:
# https://git-scm.com/download/win

# Проверьте установку:
git --version
```

#### PostgreSQL
```powershell
# Скачайте с:
# https://www.postgresql.org/download/windows/
# Выберите версию 14 или выше

# Во время установки:
# - Запомните пароль для пользователя postgres
# - Порт по умолчанию: 5432
```

### macOS:

```bash
# Установите Homebrew (если еще не установлен):
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установите Node.js:
brew install node@18

# Установите Git:
brew install git

# Установите PostgreSQL:
brew install postgresql@14
brew services start postgresql@14
```

### Linux (Ubuntu/Debian):

```bash
# Обновите систему:
sudo apt update

# Установите Node.js 18:
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Установите Git:
sudo apt install git

# Установите PostgreSQL 14:
sudo apt install postgresql-14 postgresql-contrib
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

---

## 3️⃣ КЛОНИРОВАНИЕ ПРОЕКТА

### Шаг 1: Откройте терминал

**Windows:** `PowerShell` или `CMD`  
**macOS/Linux:** `Terminal`

### Шаг 2: Перейдите в нужную директорию

```bash
# Например, в папку Documents:
cd ~/Documents

# Или создайте новую папку для проектов:
mkdir projects
cd projects
```

### Шаг 3: Клонируйте репозиторий

```bash
git clone https://github.com/Romslav/max-loyalty.git
```

### Шаг 4: Перейдите в папку проекта

```bash
cd max-loyalty
```

---

## 4️⃣ НАСТРОЙКА BACKEND

### Шаг 1: Перейдите в папку backend

```bash
cd backend
```

### Шаг 2: Установите зависимости

```bash
npm install
```

⏱️ **Это займет 2-5 минут**

### Шаг 3: Создайте базу данных

#### Windows (PowerShell):
```powershell
# Подключитесь к PostgreSQL:
psql -U postgres

# Введите пароль, который указали при установке

# В консоли PostgreSQL выполните:
CREATE DATABASE max_loyalty_db;

# Создайте пользователя (опционально):
CREATE USER max_user WITH PASSWORD 'max_password';
GRANT ALL PRIVILEGES ON DATABASE max_loyalty_db TO max_user;

# Выйдите:
\q
```

#### macOS/Linux:
```bash
# Подключитесь к PostgreSQL:
sudo -u postgres psql

# Создайте базу данных:
CREATE DATABASE max_loyalty_db;

# Создайте пользователя:
CREATE USER max_user WITH PASSWORD 'max_password';
GRANT ALL PRIVILEGES ON DATABASE max_loyalty_db TO max_user;

# Выйдите:
\q
```

### Шаг 4: Настройте переменные окружения

```bash
# Скопируйте пример:
cp .env.example .env

# Откройте файл .env в текстовом редакторе:
# Windows:
notepad .env

# macOS:
open -e .env

# Linux:
nano .env
```

#### Минимальная конфигурация .env:

```env
# Database
DATABASE_URL="postgresql://max_user:max_password@localhost:5432/max_loyalty_db"

# JWT Secret (замените на свой случайный ключ)
JWT_SECRET="your-super-secret-jwt-key-change-me-12345"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-change-me-67890"

# App
PORT=3000
NODE_ENV=development

# Telegram Bot (опционально, можно оставить пустым)
TELEGRAM_BOT_TOKEN=""
TELEGRAM_WEBHOOK_URL=""

# Email (опционально для тестирования)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-app-password"
```

### Шаг 5: Запустите миграции базы данных

```bash
# Создайте Prisma Client:
npm run generate

# Запустите миграции:
npm run migrate
```

✅ **Если вы видите:** `Your database is now in sync with your schema.` - все прошло успешно!

### Шаг 6: (Опционально) Заполните тестовыми данными

```bash
# Запустите seed скрипт:
npm run seed
```

### Шаг 7: Запустите backend сервер

```bash
npm run start:dev
```

✅ **Вы должны увидеть:**
```
[Nest] Application successfully started on port 3000
```

**Не закрывайте это окно терминала!** Backend должен работать постоянно.

---

## 5️⃣ НАСТРОЙКА FRONTEND

### Шаг 1: Откройте НОВОЕ окно терминала

⚠️ **Важно:** Backend должен продолжать работать в первом терминале!

### Шаг 2: Перейдите в корневую папку проекта

```bash
# Если вы были в backend, вернитесь назад:
cd ..

# Убедитесь, что вы в max-loyalty (корень проекта):
pwd  # macOS/Linux
cd   # Windows - покажет текущий путь
```

### Шаг 3: Установите зависимости frontend

```bash
npm install
```

⏱️ **Это займет 2-5 минут**

### Шаг 4: Настройте переменные окружения

```bash
# Скопируйте пример:
cp .env.example .env.local

# Откройте .env.local:
# Windows:
notepad .env.local

# macOS:
open -e .env.local

# Linux:
nano .env.local
```

#### Минимальная конфигурация .env.local:

```env
# API Configuration
VITE_API_URL=http://localhost:3000/api
VITE_API_TIMEOUT=10000

# Application
VITE_APP_NAME=Max Loyalty
VITE_APP_VERSION=1.0.0
VITE_ENVIRONMENT=development

# WebSocket
VITE_WS_URL=ws://localhost:3000
VITE_WS_RECONNECT_INTERVAL=5000

# Features
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_PUSH_NOTIFICATIONS=false
VITE_ENABLE_OFFLINE_MODE=false

# Logging
VITE_LOG_LEVEL=info
VITE_LOG_TO_CONSOLE=true
VITE_LOG_TO_SENTRY=false

# Sentry (опционально, оставьте пустым)
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development
```

### Шаг 5: Запустите frontend сервер

```bash
npm run dev
```

✅ **Вы должны увидеть:**
```
  VITE v5.0.0  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h + enter to show help
```

---

## 6️⃣ ПЕРВЫЙ ЗАПУСК

### Шаг 1: Откройте браузер

Перейдите по адресу: **http://localhost:5173**

### Шаг 2: Вы должны увидеть страницу входа

✅ Если видите форму логина - **все работает!**

---

## 7️⃣ ТЕСТОВЫЕ АККАУНТЫ

### После запуска seed (если вы его выполнили):

#### 👑 Администратор
```
Email: admin@maxloyalty.com
Password: admin123
```
**Доступ:** Полный доступ ко всей системе

#### 🏪 Владелец ресторана
```
Email: owner@restaurant.com
Password: owner123
```
**Доступ:** Управление рестораном, аналитика, гости

#### 💼 Кассир
```
Email: cashier@restaurant.com
Password: cashier123
```
**Доступ:** Сканирование карт, начисление баллов

#### 👤 Гость
```
Email: guest@example.com
Password: guest123
```
**Доступ:** Просмотр своей карты, баллов, истории

---

## 8️⃣ ПРОВЕРКА РАБОТОСПОСОБНОСТИ

### ✅ Checklist после запуска:

- [ ] Backend работает на http://localhost:3000
- [ ] Frontend работает на http://localhost:5173
- [ ] Страница входа загружается
- [ ] Можете войти с тестовым аккаунтом
- [ ] Dashboard загружается после входа
- [ ] API Swagger доступен: http://localhost:3000/api/docs

### Тест API:

Откройте в браузере: **http://localhost:3000/api/health**

✅ **Ожидаемый ответ:**
```json
{
  "status": "ok",
  "timestamp": "2026-01-21T14:00:00.000Z"
}
```

---

## 9️⃣ УСТРАНЕНИЕ ПРОБЛЕМ

### Проблема: Backend не запускается

#### Ошибка: "Cannot connect to database"

**Решение:**
```bash
# Проверьте, что PostgreSQL запущен:
# Windows:
services.msc  # Найдите PostgreSQL, проверьте статус

# macOS:
brew services list | grep postgresql

# Linux:
sudo systemctl status postgresql

# Проверьте DATABASE_URL в backend/.env
# Убедитесь что порт, пользователь и пароль правильные
```

#### Ошибка: "Port 3000 is already in use"

**Решение:**
```bash
# Найдите процесс на порту 3000:
# Windows:
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# macOS/Linux:
lsof -i :3000
kill -9 <PID>

# Или измените порт в backend/.env:
PORT=3001
```

### Проблема: Frontend не запускается

#### Ошибка: "Port 5173 is already in use"

**Решение:**
```bash
# Закройте процесс или используйте другой порт:
npm run dev -- --port 5174
```

#### Ошибка: "Module not found"

**Решение:**
```bash
# Удалите node_modules и переустановите:
rm -rf node_modules package-lock.json
npm install
```

### Проблема: Не могу войти

**Решение:**
```bash
# Проверьте:
1. Backend запущен и доступен
2. В консоли браузера (F12) нет ошибок
3. Проверьте Network tab - идет ли запрос к API
4. Убедитесь что VITE_API_URL правильный в .env.local

# Пересоздайте базу данных:
cd backend
npm run migrate:reset  # ОСТОРОЖНО: удалит все данные!
npm run seed
```

### Проблема: База данных пустая

**Решение:**
```bash
cd backend

# Запустите seed снова:
npm run seed
```

### Проблема: Ошибки TypeScript

**Решение:**
```bash
# Проверьте типы:
npm run type-check

# Если ошибки в node_modules, попробуйте:
rm -rf node_modules package-lock.json
npm install
```

---

## 🎓 СЛЕДУЮЩИЕ ШАГИ

### После успешного запуска:

1. **Изучите документацию:**
   - [README.md](./README.md) - Обзор проекта
   - [SETUP.md](./SETUP.md) - Детальная настройка
   - [DEVELOPMENT.md](./DEVELOPMENT.md) - Разработка
   - [ROADMAP.md](./ROADMAP.md) - План развития

2. **Изучите код:**
   ```
   src/
   ├── pages/        # Начните отсюда - страницы приложения
   ├── components/   # Переиспользуемые компоненты
   ├── stores/       # State management (Zustand)
   └── services/     # API сервисы
   ```

3. **Попробуйте функции:**
   - Войдите как разные роли
   - Создайте новую карту гостя
   - Посмотрите аналитику
   - Проверьте настройки уведомлений

4. **Запустите тесты:**
   ```bash
   npm run test
   ```

---

## 📚 ПОЛЕЗНЫЕ КОМАНДЫ

### Backend:
```bash
cd backend

npm run start:dev      # Запуск в режиме разработки
npm run start:prod     # Запуск production build
npm run build          # Собрать production
npm run test           # Запустить тесты
npm run migrate        # Применить миграции
npm run migrate:reset  # Сбросить базу данных
npm run seed           # Заполнить тестовыми данными
npm run generate       # Сгенерировать Prisma Client
```

### Frontend:
```bash
npm run dev            # Запуск dev сервера
npm run build          # Сборка для production
npm run preview        # Просмотр production build
npm run lint           # Проверка кода
npm run lint:fix       # Исправить ошибки линтера
npm run format         # Форматировать код
npm run type-check     # Проверить типы TypeScript
npm run test           # Запустить тесты
npm run test:coverage  # Тесты с покрытием
```

---

## 🆘 ПОЛУЧИТЬ ПОМОЩЬ

### Если ничего не помогло:

1. **Проверьте Issues на GitHub:**
   https://github.com/Romslav/max-loyalty/issues

2. **Создайте новый Issue:**
   Включите:
   - Описание проблемы
   - Шаги для воспроизведения
   - Версию Node.js (`node --version`)
   - Версию npm (`npm --version`)
   - Операционную систему
   - Скриншоты ошибок

3. **Логи:**
   ```bash
   # Backend логи:
   cd backend
   npm run start:dev 2>&1 | tee backend.log

   # Frontend логи:
   npm run dev 2>&1 | tee frontend.log
   ```

---

## ✅ ФИНАЛЬНЫЙ CHECKLIST

- [ ] Node.js 18+ установлен
- [ ] PostgreSQL 14+ установлен и запущен
- [ ] Git установлен
- [ ] Проект склонирован
- [ ] Backend зависимости установлены
- [ ] Frontend зависимости установлены
- [ ] База данных создана
- [ ] Миграции выполнены
- [ ] Seed данные загружены (опционально)
- [ ] Backend запущен на :3000
- [ ] Frontend запущен на :5173
- [ ] Можете войти в систему
- [ ] API отвечает на /api/health

---

**🎉 ГОТОВО! Теперь вы готовы к работе с Max Loyalty!**

---

**Создано:** 21 января 2026  
**Версия:** 1.0.0  
**Автор:** Romslav
