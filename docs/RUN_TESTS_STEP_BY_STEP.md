# 🚀 Пошаговое руководство по запуску тестов

## ШАГ 1: Подготовка

### 1️⃣ Откройте терминал

Откройте терминал в вашем браузере или IDE.

### 2️⃣ Перейдите в проект

```bash
cd max-loyalty
```

### 3️⃣ Установите зависимости

Проверьте, что все зависимости установлены:

```bash
npm install
# или если используете yarn:
yarn install
# или pnpm:
pnpm install
```

Нормальный вывод:
```
> npm install
Adding packages...

✓ 1 package installed successfully
```

---

## ШАГ 2: Первый запуск тестов

### Простая команда - запустить все тесты

```bash
npm run test
```

👍 **Рекомендуется для первого запуска**

Ожидаемый вывод:

```
✔ PASS  src/hooks/__tests__/useAuth.test.ts
✔ PASS  src/hooks/__tests__/usePermissions.test.ts
✔ PASS  src/hooks/__tests__/useNotification.test.ts
✔ PASS  src/hooks/__tests__/useAsync.test.ts
✔ PASS  src/hooks/__tests__/useLocalStorage.test.ts
✔ PASS  src/pages/admin/__tests__/AuditLogs.test.tsx

 Test Files  6 passed (6)
      Tests  45 passed (45)
   Start at  18:15:20
   Duration  2.34s
```

✅ **Отлично! Все тесты пройдены!**

---

## ШАГ 3: Варианты запуска тестов

### Опция 1: Локальная разработка (RECOMMENDED)

Часто баги появляются, когда вы исправляете код. Мы можем автоматически пересчитывать тесты на каждое изменение:

```bash
npm run test:watch
```

👍 **Отлично для разработки**

Вы увидите:

```
Watching for file changes...

Press q to exit
Press p to filter by filename regex pattern
Press t to filter by test name regex
Press a to run all tests
```
Отменяйте функцию, и тесты автоматически пересчитываются!

### Опция 2: Watch режим для одного файла

Проверьте один тест в watch режиме:

```bash
npm run test:watch -- src/hooks/__tests__/useAuth.test.ts
```

### Опция 3: Конкретные тесты

Для запуска тестов логина:

```bash
npm run test -- --grep "login"
```

### Опция 4: Тесты с UI интерфейсом

Откройте красивый интерфейс (одна из лучших опций):

```bash
npm run test:ui
```

👋 **Это откроет визуальный UI в браузере**

---

## ШАГ 4: Проверка покрытия кода

### Команда

```bash
npm run test:coverage
```

### Ожидаемые результаты

```
--------|----------|----------|----------|----------|---|
File    | % Stmts  | % Branch | % Funcs  | % Lines  |---|
--------|----------|----------|----------|----------|---|
All     |   87.2   |   82.1   |   89.5   |   88.3   |   |
Auth    |   92.5   |   85.3   |   95.2   |   93.1   |   |
Permis. |   88.3   |   81.2   |   90.5   |   89.2   |   |
Notif.  |   87.1   |   79.8   |   88.9   |   87.5   |   |
Async   |   89.5   |   84.2   |   91.3   |   90.1   |   |
Storage |   91.2   |   87.5   |   93.8   |   92.1   |   |
--------|----------|----------|----------|----------|---|
```

✅ **Оценка: 87.2% объявлений строк покрыты (> 85% цель)**

### Открыть отчет в браузере

```bash
# macOS
open coverage/index.html

# или Linux
xdg-open coverage/index.html

# или Windows
start coverage\index.html
```

---

## ШАГ 5: Тест конкретных компонентов

### Тест useAuth

```bash
npm run test -- useAuth
```

Ожидать:
```
✔ PASS  src/hooks/__tests__/useAuth.test.ts (245ms)
  useAuth Hook
    login
      ✓ should login successfully with valid credentials
      ✓ should handle login error
    logout
      ✓ should clear auth data on logout
    changePassword
      ✓ should change password successfully

  Tests:  4 passed
```

### Тест usePermissions

```bash
npm run test -- usePermissions
```

### Тест useNotification

```bash
npm run test -- useNotification
```

### Тест useAsync

```bash
npm run test -- useAsync
```

### Тест useLocalStorage

```bash
npm run test -- useLocalStorage
```

### Тест AuditLogs компонента

```bash
npm run test -- AuditLogs
```

---

## ШАГ 6: Отладка и DEBUG

### Debug режим

```bash
DEBUG=* npm run test
```

### Лишние выводы

Теперь все `console.log()` отладочные выводы будут видны:

```typescript
it('should work', () => {
  console.log('Debug info here'); // Будет видно
});
```

### Verbose тесты

```bash
npm run test -- --reporter=verbose
```

---

## ШАГ 7: Проверка TypeScript

### Проверить типы

```bash
npm run type-check
```

Ожидаемый вывод:
```
TypeScript check: PASSED
No issues detected
```

---

## ШАГ 8: Полный тестовый цикл

### Запустить тесты, типы и покрытие

```bash
npm run test && npm run type-check && npm run test:coverage
```

Ожидать ~10-15 секунд.

Ожидаемые результаты:

```
✔ All tests passed
✔ TypeScript check passed
✔ Code coverage: 87.2%
```

---

## ☝️ Советы и трики

### Совет 1: Выход из watch режима

При команде `npm run test:watch`:
- Нажмите `q` чтобы выйти
- Нажмите `a` для рестарта всех тестов
- Нажмите `p` для фильтра по имени файла
- Нажмите `t` для фильтра по имени теста

### Совет 2: Пересчет тестов для измененных файлов

```bash
# Пересчитать только тесты для модифицированных файлов
npm run test -- --only-changed
```

### Совет 3: Отключить один тест

Для временного отключения теста:

```typescript
it.skip('should do something', () => {
  // Этот тест будет пропущен
});
```

### Совет 4: Запустить только один тест

```typescript
it.only('should do something', () => {
  // ТОЛЬКО этот тест запустится, никакие другие
});
```

---

## 🚨 Ошибки и Отладка

### Проблема: Не найдены тесты

```bash
# Ошибка
# No test files found

# Решение: Убедитесь, что тесты заканчиваются на .test.ts или .test.tsx
ls src/**/__tests__/*.test.*
```

### Проблема: Порт занят

```bash
# Ошибка
# Cannot bind to port 51204

# Решение: Автоматически выбрать другой порт
# npm run test:ui автоматически выберет свободный
```

### Проблема: Моки не работают

```typescript
// Ошибка: Cannot find module or its type declaration

// Решение: Обеспечить vi.mock() в верхней части файла
vi.mock('@/services/api');
vi.mock('react-hot-toast');
```

---

## ✅ Проверка полноты

### После выполнения всех шагов отметьте:

- [ ] Все тесты проходят (`npm run test`)
- [ ] Watch режим работает (`npm run test:watch`)
- [ ] UI режим открывается (`npm run test:ui`)
- [ ] Покрытие >85% (`npm run test:coverage`)
- [ ] TypeScript не дает ошибок (`npm run type-check`)

✅ **Поздравляю! Вы готовы к запуску тестов!**

---

## 🌔 Следующие шаги

В следующие дни:

### Версия 3 (Автоматическая проверка)

- [ ] Zod валидация для всех форм
- [ ] XSS защита
- [ ] CSRF защита
- [ ] аудит безопасности

### Версия 4 (Production)

- [ ] Production build
- [ ] Docker конфигурация
- [ ] Мониторинг
- [ ] Развертывание
